# Mac に Node.js を入れる

> **これも家で1人でやるための手順です。** ダウンロードに時間がかかるので、10〜15分みておいてください。

**Node.js とは何か**は次回の授業で少し触れます。**今は「次回使う道具」だと思って入れてください。**

**手順**

1. ブラウザで **https://nodejs.org/en/download** を開く
2. 画面の上のほうに、**バージョン・OS・インストール方法**を選ぶところがあります。次のとおりに選んでください
   - バージョン … **LTS** と書いてあるほう（2026年9月時点で `v24.21.0`）
   - OS … **macOS**
   - インストール方法 … **Prebuilt Installer**

   > **`nvm` や `Homebrew` は選ばないでください。** どちらもターミナルで操作する入れ方で、今日はそこまで要りません。**選ぶところが見当たらなければ、そこで止めて連絡してください。**

3. ダウンロードしたファイル（`.pkg`）を開いて、画面の指示に沿って進める
4. 終わったら、**確認する**（次が大事）

**確認のしかた**

1. VS Code を開く
2. 上のメニューから「ターミナル」→「新しいターミナル」
3. 下に出てきた黒い（または白い）欄に、次のとおり打って `return` を押す

```
node -v
```

4. `v24.21.0` のような**数字が表示されれば成功**です

**うまくいかないとき**

| 表示 | どうするか |
|---|---|
| `command not found: node` | VS Code をいったん終了して、もう一度開いてから確認し直す。それでも出なければ、手順2からやり直す |
| インストーラが「開けません」と出る | 「システム設定」→「プライバシーとセキュリティ」を開き、下のほうに出ている「このまま開く」を押す |
| 数字が `v20` など、24より小さい | そのままでも動くかもしれませんが、**念のため連絡してください** |

**この確認（`node -v` で数字が出る）まで済ませて、次回に来てください。**
