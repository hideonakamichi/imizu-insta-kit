# Claude Code にスキルを入れる（Mac）

## スキルとは何か

**「この作業はこう進めてほしい」という手順を、日本語で書いたファイル**です。決まった置き場所に入れておくと、Claude Code が必要なときに自分で読んでくれます。

中身はプログラムではありません。**開けば全部読めます。** 気に入らないところは書き換えてください。**書き換えることが、このスクールでやってほしいことです。**

## 入れ方（10分ほど）

### 1. ダウンロードする

このページの上のほうにある緑色の **`Code`** ボタン → **`Download ZIP`** を押します。

`ダウンロード` フォルダに `imizu-insta-kit-main.zip` が入ります。ダブルクリックすると展開されます。

### 2. 置き場所を作る

Claude Code のスキルは、**決まった場所に置かないと読まれません。** その場所を作ります。

VS Code で Claude Code を開き、そのまま日本語で頼んでください。

```
~/.claude/skills と ~/.claude/agents というフォルダを作ってください。
```

### 3. コピーする

続けて、こう頼みます。`（展開したフォルダのパス）` のところは、フォルダを Claude Code の画面にドラッグすると入ります。

```
（展開したフォルダのパス）の中の skills フォルダの中身を ~/.claude/skills へ、
agents フォルダの中身を ~/.claude/agents へコピーしてください。
```

### 4. 確認する

VS Code を一度閉じて、開き直します。Claude Code に `/` と打つと、入れたスキルの名前が並びます。

出てこないときは、**そのまま Claude Code に聞いてください。**

```
~/.claude/skills に入れたスキルが出てきません。どこを見ればいいですか。
```

## 入っているもの

| 名前 | 何をするもの |
|---|---|
| `creating-visual-explainers` | 図解を1枚作る |
| `grill-me` | 作る前に、1問ずつ決めていく |
| `ask-mode` | 実行せずに、情報の整理だけする |
| `writing-proposals` | 提案文を1本書く |
| `writing-slide-decks` | スライドの設計図を作る |
| `writing-presentations` | 発表の読み上げ原稿を作る |
| `judge-panel` | 作ったものを、別の視点から点検させる |

`agents` フォルダは `judge-panel` が呼び出す点検役の定義です。単体では使いません。

## そのままでは動かないところがあります

**これはわざとです。** 中道さんの環境に合わせて書かれた部分が残っています。

- **色の指定**：図解とスライドのスキルは、中道さんの会社のルールファイル（`~/.cursor/...`）を見にいきます。**そのファイルは入っていません**
- **Windows 用の手順**：`powershell` と書かれたところは Mac では動きません
- **社内向けの前提**：提案文のスキルは「直属上長に出す」前提で書かれています

**ここを自分に合わせて直すのが、このスクールの後半でやることです。** 直し方が分からなければ、そのファイルを開いて Claude Code に聞いてください。

```
このスキルの（該当箇所）は、私の環境では動きません。
射水市の Instagram 発信に合わせて直したいです。どう書き換えればいいですか。
```

## 気をつけること

**スキルを直したら、なぜそう直したかを一言メモしておいてください。** 半年後に自分で読み返したときに効きます。

**全部を一度に直そうとしないでください。** 使ってみて「ここが合わない」と思ったところだけ、1つずつ直します。
